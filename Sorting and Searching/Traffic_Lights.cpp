#include "bits/stdc++.h";
using namespace std;

vector<vector<int>> genrateSpiralMatrix(int x){
    vector<vector<int>>matrix;
    for(int i=0;i<x;i++){
        for(int j=0;j<x;j++){
            if(i%2 == 0 ){}
        }
    }
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int y, x;cin >> y >> x;
    int x = max(y,x);
    vector<vector<int>> res = genrateSpiralMatrix(x);
    cout<<
    return 0;
}