#include "bits/stdc++.h"
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);cout.tie(nullptr);
    int tcs;cin>>tcs;
    while(tcs--){
        int n,cnt=0;cin >> n;
        string s,r;cin >> s >> r;
        for(int i=0;i<n;i++){
            cnt++;
            if(s[i] != s[i+1]){
                s[i]
            }
        }
    }
    return 0;
}